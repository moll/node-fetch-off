{
	"name": "fetch-off",
	"version": "1.3.0",
	"description": "Fetch API polyfill and facade. Its request and response wrappers can be used separately: use fetch for request yet get back Node's Http.get response (IncomingMessage) for streaming.",
	"keywords": [
		"fetch",
		"http",
		"request"
	],
	"homepage": "https://github.com/moll/node-fetch-off",
	"bugs": "https://github.com/moll/node-fetch-off/issues",

	"author": {
		"name": "Andri Möll",
		"email": "andri@dot.ee",
		"url": "http://themoll.com"
	},

	"repository": {
		"type": "git",
		"url": "git://github.com/moll/node-fetch-off.git"
	},

	"licenses": [{
		"type": "LAGPL",
		"url": "https://github.com/moll/node-fetch-off/blob/master/LICENSE"
	}],

	"main": "fetch.js",
	"scripts": {"test": "make test"},

	"dependencies": {
    "node-fetch": ">= 1.3.0 < 2",
    "oolong": ">= 1 < 2",
    "lazy-object": ">= 1 < 2"
	},

	"devDependencies": {
		"mocha": ">= 2.5.3 < 3",
		"co-mocha": ">= 1.0.0 < 2",
		"must": ">= 0.13.2 < 0.14",
		"mitm": "^1.3.2",
		"sinon": "^1.17.6"
	},

	"engines" : {"node": ">= 4"}
}
